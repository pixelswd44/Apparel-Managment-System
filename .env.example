# Copy this file to .env and fill in your values

# Required: Secret key for JWT tokens (use a long random string)
JWT_SECRET=your-very-long-random-secret-key-here

# Optional: Port (Hostinger sets this automatically via PORT env var)
# PORT=3001

# Optional: Environment
NODE_ENV=production
